{
    "buildCommand": "cd client && yarn install && ./node-modules/vite/bin/vite.js build",
    "outputDirectory" : "client/dist",
    "rewrites":[
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        }
    ]
}