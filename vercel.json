{
    "buildCommand": "cd client && npm install && ./node-modules/vite/bin/vite.js build",
    "outputDirectory" : "client/dist",
    "frameworks" : "vite",
    "rewrites":[
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        }
    ]
}